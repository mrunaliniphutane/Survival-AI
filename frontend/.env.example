# Copy this to frontend/.env.local for local development
# VITE_API_URL is NOT set locally (the Vite proxy handles /api → localhost:5000)

# For Vercel deployment, set VITE_API_URL to your Render backend URL:
# VITE_API_URL=https://survival-ai-backend.onrender.com/api
